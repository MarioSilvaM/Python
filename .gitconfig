[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = Mario Silva
	email = mariosilva924@gmail.com
[core]
	editor = code --wait
	autocrlf = true
